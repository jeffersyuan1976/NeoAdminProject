---
description: NeoAdmin 业务开发时优先使用 ILogger 结构化日志，便于事后排查
alwaysApply: true
---

# 日志输出约定

项目已接入 Serilog（`AddNeoAdminSerilog`）。**新增或修改业务代码时，应主动补充日志**，不要等出问题再补。

完整说明见 `NEOADMIN开发上手.md` §3.4；在线查看：`/admin/system-log`。

## 必须打日志的场景

| 场景 | 级别 | 示例 |
|------|------|------|
| API / 定时任务 / Service 入口与正常结束 | `Information` | `查询开始`、`处理完成，Count={Count}` |
| 用户触发的保存、删除、导入导出 | `Information` | `保存成功，Id={Id}` |
| 可恢复异常、数据为空、校验失败 | `Warning` | `列表为空`、`参数无效，Field={Field}` |
| `catch`、外部调用失败、数据不一致 | `Error` | `LogError(ex, "导入失败，File={File}", file)` |

## 各层注入方式

```razor
@inject ILogger<MyPage> Logger
```

```csharp
// API（继承 BaseApiController）
Logger.LogInformation("...");

// Service
public MyService(ILogger<MyService> logger) { ... }

// Jobs 静态方法
var logger = sp.GetRequiredService<ILoggerFactory>().CreateLogger(typeof(MyJobs));
```

## 写法要求

- 用 `{占位符}` 传参，**禁止** `$"..."` 拼接
- 异常必须 `LogError(ex, "消息", args...)`
- 日志消息用中文或英文均可，同一模块内保持一致
- **不要记录**：密码、Token、完整 Cookie、身份证号等敏感字段

## 新增文件检查清单

新建或大幅改动以下文件时，确认已注入 `ILogger` 并在关键路径有日志：

- `Components/**/*.razor`（含 `OnSaveAsync`、自定义按钮、复杂查询）
- `Api/**/*.cs`
- `Jobs/**/*.cs`
- `Services/**/*.cs`

演示页可用 `DemoPageLog`；**业务代码用 `ILogger<T>`**。
