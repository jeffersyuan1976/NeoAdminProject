---
description: NeoAdmin REST API 与定时任务开发约定
globs: "{**/Api/**/*.cs,**/Jobs/**/*.cs}"
alwaysApply: false
---

# REST API 与定时任务

## REST API（宿主 Api/）

`Program.cs` 注册：`builder.Services.AddNeoAdminApi(Assembly.GetExecutingAssembly());`

```csharp
[ApiController]
[Route("api/article")]
public sealed class ArticleController : ControllerBase
{
    [HttpGet($"@{nameof(GetAll)}")]
    [AllowAnonymous]
    public async Task<ApiResult<ArticleListResponse>> GetAll() { ... }
}
```

- 路由约定：`[HttpGet($"@{{nameof(MethodName)}}")]`
- 权限对应菜单 `Api` 类型：`MenuPermissionService.HasApiAsync("组名", "动作名")`
- Swagger：开发环境 `/api`（`NeoAdmin:IsSwagger`）

## 定时任务（宿主 Jobs/）

```csharp
[Scheduler("blog.sync-article-stats", "0 0 3 * * *")]
public static async Task SyncArticleStats(IServiceProvider sp, TaskInfo task)
{
    var freeSql = sp.GetRequiredService<IFreeSql>();
    var logger = sp.GetRequiredService<ILoggerFactory>().CreateLogger(typeof(BlogJobs));
    logger.LogInformation("SyncArticleStats 开始，TaskId={TaskId}", task.Id);
    // 业务逻辑
    logger.LogInformation("SyncArticleStats 完成");
}
```

`AddNeoAdmin` 中扫描宿主程序集：

```csharp
options.SchedulerAssemblies = [Assembly.GetExecutingAssembly()];
```

管理界面：`/admin/task-scheduler`

## 常用注入服务

| 服务 | 用途 |
|------|------|
| `IFreeSql` | 数据访问 |
| `ILogger<T>` | 结构化日志（入口/结束/异常，见 `neoadmin-logging` 规则） |
| `IToastService` | 轻提示 |
| `DialogService` | 确认对话框 |
| `MenuPermissionService` | 权限 |
| `NeoAdminAuthService` | 当前用户 |
| `AuditWorkflowService` | 审批流 |
| `FileService` | 文件上传 |
