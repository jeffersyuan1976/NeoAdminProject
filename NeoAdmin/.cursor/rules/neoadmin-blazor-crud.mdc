---
description: NeoAdmin Blazor 页面、CrudTable 与路由菜单约定
globs: "**/*.razor"
alwaysApply: false
---

# Blazor 页面与 CrudTable

## 页面骨架

```razor
@page "/MyModule/Item"
@layout LayoutAdmin
@using NeoAdmin.Blazor.Components
@inject IFreeSql FreeSql
@inject ILogger<MyItem> Logger

<div class="flex flex-col flex-1 min-h-0 overflow-hidden">
    <CrudTable TItem="MyItem" Title="示例" PageSize="20"
               InitQuery="InitQuery" OnQuery="HandleQuery">
        <Columns>
            <DataTableColumn TData="CrudGridRow<MyItem>" TValue="string"
                Property="@(row => row.Item.Name)" Header="名称" Sortable="false" />
        </Columns>
        <EditTemplate Context="item">
            <div class="space-y-2">
                <Label For="item-name">名称</Label>
                <Input id="item-name" Class="w-full" @bind-Value="item.Name" />
            </div>
        </EditTemplate>
    </CrudTable>
</div>

@code {
    private Task InitQuery(CrudQueryInfo e) { e.Filters = []; return Task.CompletedTask; }
    private void HandleQuery(CrudQueryEventArgs<MyItem> e) =>
        e.Select!.WhereIf(!string.IsNullOrWhiteSpace(e.SearchText),
            a => a.Name.Contains(e.SearchText!)).OrderByDescending(a => a.Id);
}
```

- 后台页用 `LayoutAdmin`；登录等用 `LayoutEmpty`
- **菜单 `SysMenu.Path` = `@page` 路由 = `CrudTable.MenuPath`**（默认当前路由）

## CrudTable 关键参数

| 参数 | 说明 |
|------|------|
| `InitQuery` / `OnQuery` | 筛选器配置与 FreeSql 条件 |
| `OnSaveAsync` / `OnDeleteAsync` / `LoadFunc` | 覆盖默认 CRUD |
| `IsWorkflowAudit` + `WorkflowAuditMenuPath` | 审批流 |
| `Compact` / `IsPickerMode` | 嵌入选择器 |
| `SplitPane` + 双 `CrudTable` | 左右分栏（参考框架 `Dict.razor`） |

## 筛选器 InitQuery 示例

```csharp
e.Filters =
[
    new CrudFilterInfo("状态", "status", "全部,启用,禁用", ",-1,1"),
    new CrudFilterInfo("类型", "type", typeof(MyEnum)),
    new CrudFilterInfo("创建时间", "date", CrudFilterType.DateRange, 12),
];
```

## 权限（CrudTable 内置）

- 新增 → `add`；编辑 → `edit`；删除 → `remove`
- 代码校验：`MenuPermissionService.HasPageAsync` / `HasButtonAsync` / `HasApiAsync`
- 管理员角色跳过校验

## NeoAdmin 组件选型

| 场景 | 组件 |
|------|------|
| 标准 CRUD | `CrudTable` |
| 字典下拉 | `NeoSelectDict` |
| 枚举下拉 | `NeoSelectEnum<T>` |
| 实体下拉 | `NeoSelectEntity<TItem,TKey>` |
| 弹窗选实体 | `NeoInputTable`（须 LayoutAdmin） |
| 主从分配 | `NeoAllocTable` |
| 审批面板 | `CrudAuditPanel` |

## NeoUI 表单

优先 `Field` + `Label` + `Input`；简单场景 `div.space-y-2` + `Label` + `Input`。

演示页：`/neo-demo/ui/*`（NeoUI）、`/neo-demo/comp/*`（NeoAdmin 组件）、`/Blog/*`（业务示例）。

全局 `_Imports.razor` 通常含：`NeoAdmin.Blazor.Components`、`NeoUI.Blazor`、`NeoUI.Icons.Lucide`。

## 日志

自定义 `OnSaveAsync` / `OnDeleteAsync` / 按钮事件时，用 `Logger.LogInformation` 记录操作，`catch` 中用 `Logger.LogError(ex, ...)`。详见 `neoadmin-logging` 规则。
